# 终端台设计与任务规划

## 目标

终端台用于在同一个页面内完成串口监视、串口交互和文本协议调试。第一阶段先覆盖常用串口终端能力：串口连接、监听重连、标准终端输入、文本终端发送、编码切换、流控、时间戳、16 进制显示、自动染色、复制与清屏。

## 使用场景

1. 标准终端模式
   - 面向 Linux 串口控制台、Bootloader shell、AT shell、MicroPython REPL 等交互。
   - 输入框按 Enter 发送一行，接收区按设备输出连续追加。
   - 切换到文本终端模式时不关闭串口，已有日志保留。

2. 文本终端模式
   - 面向纯文本协议调试。
   - 使用多行发送框输入文本，点击发送后按当前编码写入串口。
   - 适合发送固定命令、多行文本、带 CRLF 默认换行的 payload。

## 第一阶段功能边界

- 支持即时切换“标准终端 / 文本终端”，只切换输入区和日志显示策略，不重连串口。
- 串口下拉框保留扫描到的串口，同时提供自定义串口输入。
- 点击“连接”默认自动选中“监听”；监听期间设备掉线后按最后一次连接参数自动重连。
- 支持即时切换文本编码，历史记录按原始字节重新渲染。
- 支持常用编码：UTF-8、GBK、GB18030、Big5、Shift-JIS、Latin-1、ASCII，并允许手动输入 Python codec 名称。
- 支持基本串口参数：
  - 串口号
  - 波特率
  - 数据位 5/6/7/8
  - 校验位 None / Even / Odd / Mark / Space
  - 停止位 1 / 1.5 / 2
  - 流控 None / XON/XOFF / RTS/CTS / DSR/DTR
- 支持接收/发送时间记录：
  - 开启后每条 RX/TX 记录带本地时间、方向、字节数。
  - 关闭后标准终端更接近原始终端输出，普通终端仍保留方向提示。
- 支持 16 进制显示：
  - 全局开关用于快速切换默认显示。
  - 日志内部保留原始字节，因此编码和 HEX 显示切换不丢信息。
  - 开启时间戳后，单条 RX/TX 记录会显示 `HEX / 文本` 局部切换入口，不需要整屏切换。
- 接收区使用只读文本控件，支持鼠标选择和复制。
- 接收区提供“自动跟随”开关：
  - 默认开启，日志到达时自动保持在底部。
  - 用户向上拖动滚动条后自动关闭，后续串口内容追加时当前视野保持相对静止。
  - 用户拖回底部或点击“自动跟随”后恢复自动贴底。
- 终端会话面板内置会话标签页，并在标签页右侧提供“新建会话”入口：
  - 会话标签以串口号为名称；未配置串口时显示“会话 N”。
  - 点击标签页可回到该会话之前的终端聊天记录。
  - 串口参数、终端模式、编码、换行、时间戳、HEX、自动跟随、监听状态和发送历史都随会话保存。
  - 每个会话拥有独立串口线程；点击“断开”只断开当前标签页会话。
- 日志自动按常见状态字符串染色，例如 error/fail/warn/ok/ready/connected/复位/监听/0x 地址。
- 支持清空日志、复制全部、刷新串口、连接/断开。
- 发送框支持使用键盘上/下方向键切换历史发送内容。

## 交互结构

页面采用三段式操作布局：

1. 顶部终端会话区
   - 标题、会话数量、新建会话、连接状态、显示开关和清理/复制操作。
   - 会话标签页与串口配置合并在同一面板，不再拆成“终端会话 / 监控会话”两块。
   - 模式、串口、自定义串口、刷新、波特率、数据位、校验位、停止位、流控、连接状态、监听。

2. 中部日志区
   - 只读、等宽字体、可选择复制。
   - 记录来源分为 `SYS`、`RX`、`TX`。
   - 编码/HEX/时间戳变化时由原始日志记录重新渲染。

3. 底部输入区
   - 标准终端：单行输入，Enter 发送。
   - 文本终端：多行输入，按钮发送。
   - 两种发送窗口的输入框都填满发送区域；发送按钮位于输入框右下角。
   - 标准终端的底部输入框占满高度，提示文本位于左上角。
   - 标准终端模式下可直接聚焦终端输出区输入，按 Enter 发送。
   - 两种模式共用编码和换行策略，默认换行为 CRLF。
   - 上/下方向键调用最近 100 条发送历史。

## 技术设计

### 数据模型

终端日志记录以原始字节为核心：

```text
TerminalLogRecord
- direction: SYS | RX | TX
- timestamp: datetime
- payload: bytes
- message: str
```

设计要点：

- RX/TX 保存原始字节，显示时再按当前编码解码。
- SYS 保存状态文本，例如“串口已打开”“串口错误”。
- 切换编码或 HEX 时不改记录，只重新渲染视图。
- `TerminalSession` 保存一组会话属性和历史，包括串口配置、显示选项、发送历史、日志记录、监听状态、串口线程和单条 HEX 覆盖状态。

### 串口线程

串口读写放在独立线程，避免阻塞 PyQt 主线程：

- 打开串口时读取当前参数和流控配置。
- 读线程持续读取 `in_waiting`，收到数据后通过 Qt signal 回主线程。
- 发送数据通过线程安全队列交给串口线程写入。
- 断开时设置停止事件，线程关闭串口并发出关闭信号。
- 串口线程归属具体会话；断开/监听/自动重连只作用于该会话。

### 编码策略

- 发送：按当前编码将输入文本编码为 bytes，编码不存在或字符不可编码时提示错误。
- 接收：按当前编码解码 bytes，错误字符使用替换符，不中断会话。
- 历史：编码切换后重新渲染所有 RX/TX 记录。

### 显示策略

- 标准终端 + 无时间戳 + 非 HEX：RX 近似原始输出连续追加，TX 不强制回显。
- 文本终端或开启时间戳/HEX：每条记录独立成块，带方向、字节数和内容。
- HEX 显示格式为按空格分隔的两位大写十六进制字节。
- 显示层使用 HTML pre 渲染，保留等宽排版，并对常见状态字符串做自动染色。

## 任务规划

### 阶段 1：基础可用终端

- [x] 完善本设计文档。
- [x] 新增 `TerminalWidget`。
- [x] 替换主窗口终端台占位页。
- [x] 实现串口扫描、连接、断开。
- [x] 实现自定义串口输入。
- [x] 实现监听按钮和掉线自动重连。
- [x] 实现标准终端/文本终端即时切换。
- [x] 实现编码即时切换与历史重新渲染。
- [x] 实现流控参数。
- [x] 实现收发时间记录。
- [x] 实现 16 进制显示。
- [x] 实现自动字符串染色。
- [x] 实现日志滚动条可拖动与自动跟随控制。
- [x] 移除监控会话列表，改为终端会话标题栏标签页。
- [x] 在会话标签右侧提供新建会话入口。
- [x] 实现每会话独立连接、断开、监听和自动重连。
- [x] 实现会话属性随会话保存和恢复。
- [x] 实现点击会话标签返回历史聊天。
- [x] 实现发送历史上下键切换。
- [x] 实现复制全部、清空日志。
- [x] 运行静态编译和现有测试。

### 阶段 2：终端增强

- [ ] 支持 ANSI/VT100 转义序列渲染。
- [ ] 支持本地回显开关。
- [ ] 支持 CR/LF 可视化显示。
- [ ] 支持发送历史、快捷命令和命令收藏。
- [ ] 支持会话日志导出。

### 阶段 3：高级调试

- [ ] 支持 HEX 发送模式。
- [ ] 支持定时发送。
- [ ] 支持接收过滤、关键字高亮。
- [ ] 支持多串口会话标签。
- [ ] 支持串口参数预设。
